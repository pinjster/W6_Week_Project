{% extends "base.jinja" %}

{% block content %}
    <h1>Log in</h1>
    {{ form.hidden_tag() }}
    {{ form.email.label }} <br>
    {{ form.email(size=32) }}
    </p>
    <p>
    {{ form.password.label }} <br>
    {{ form.password(size=32) }}
    </p>
    <p>
        {{ form.remember_me.label }} <br>
        {{ form.remember_me() }}
    </p>
    <p>{{ form.submit() }}</p>
{% endblock content %}